# Project Rules

- Whenever any changes are made to the portfolio source files (`index.html`, `app.js`, `styles.css`, `profile.jpg`), the agent MUST recreate/update the `Portfolio.zip` archive using `Compress-Archive` to ensure it is always in sync.
